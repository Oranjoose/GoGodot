[gd_scene load_steps=3 format=2]

[sub_resource type="GDScript" id=1]

script/source = "extends KinematicBody2D

const UP = Vector2(0, -1)
export var Gravity = 20
export var Speed = 200
export var JumpStrength = 500

var motion = Vector2(0, 0)

func _ready():
	pass
#
#func _process(delta):
#	# Called every frame. Delta is time since last frame.
#	# Update game logic here.
#	pass

func _physics_process(delta):
	motion.y += Gravity

	if Input.is_action_pressed(\"ui_right\"):
		motion.x = Speed
	elif Input.is_action_pressed(\"ui_left\"):
		motion.x = -Speed
	else:
		motion.x = 0
		
	if self.is_on_floor():
		if Input.is_action_just_pressed(\"ui_up\"):
			motion.y = -JumpStrength
			
	motion = move_and_slide(motion, UP)
	
	#print(self.global_position)
	get_parent().position = self.global_position"

[sub_resource type="RectangleShape2D" id=2]

custom_solver_bias = 0.0
extents = Vector2( 10, 10 )

[node name="Platformer" type="KinematicBody2D" index="0"]

input_pickable = false
collision_layer = 1
collision_mask = 1
collision/safe_margin = 0.08
script = SubResource( 1 )

[node name="CollisionShape2D2" type="CollisionShape2D" parent="." index="1"]

shape = SubResource( 2 )


